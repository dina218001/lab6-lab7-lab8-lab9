#include <stdio.h>
#include <iostream>

using namespace std;

int main()
{
int a, b;
int x[16];
x[16] = { 0 };
cin » a;
a = 186;
while (a > 0)
{
b = a % 16;
x[b]++;
a = a / 16;
}
int c = 0;
for (b = 0; b<16; b++) if (x[b] != 0) c++;
cout « c;
}
